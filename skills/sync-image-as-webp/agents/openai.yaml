interface:
  display_name: "Sync Image as WebP"
  short_description: "Convert PNG trees to lossless WebP"
  default_prompt: "Use $sync-image-as-webp to convert a PNG directory tree to lossless WebP while preserving structure and timestamps."
