interface:
  display_name: "Sync Image as WebP"
  short_description: "Sync a directory tree to target, converting PNG to WebP"
  default_prompt: "Use $sync-image-as-webp to sync a source directory to a target, converting PNG files to lossless WebP while copying non-PNG files unchanged."
